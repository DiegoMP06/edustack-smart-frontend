---
import Layout from "@/layouts/Layout.astro";
import { type BreadcrumbItem } from "@/types";

const breadcrumbs: BreadcrumbItem[] = [
    {
        title: "Inicio",
        href: "/",
    },
];
---

<Layout title="Inicio" breadcrumbs={breadcrumbs} />
